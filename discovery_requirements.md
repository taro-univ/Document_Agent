# Discovery Requirements
> Web全体から最適なリソースを特定・分類し、構造化データとして集約するパイプラインの要件定義

---

## 1. 目的

ユーザーの抽象的なリサーチ項目に対し、Web全体（公式ドキュメント、テックブログ、ニュース）から最適なリソースを特定・分類し、構造化データとして集約する。

---

## 2. 探索・分類戦略

| 戦略 | 概要 |
|---|---|
| **探索精度 (Scope)** | ハルシネーション抑制のため、検索結果から上位3〜5件の「高信頼ソース」を厳選する |
| **動的ラベリング (Dynamic Labeling)** | LLMがコンテンツの内容から最適なカテゴリを自動生成（例: `benchmarks`, `api_reference`, `pricing_2026`）しフォルダ分けを行う |
| **半自動パイプライン (Human-in-the-loop)** | 提案リストをユーザーが承認してから解析へ進む（後述） |
| **マルチドメイン検索** | 公式サイト (`site:docs.github.com`) だけでなく、開発者コミュニティや最新ニュースを横断して検索する |

---

## 3. システムアーキテクチャ

### 全体フロー

```
ユーザー入力（自然言語クエリ）
        │
        ▼
┌─────────────────────┐
│  Step 1: discovery.py│  ← 検索API呼び出し（Brave / Google）
│  検索 & 候補提案     │     セマンティック評価・ラベリング
└────────┬────────────┘
         │  DiscoveryProposal リストを提示
         ▼
┌─────────────────────┐
│  Human-in-the-loop  │  ← ユーザーが候補を確認・承認
│  承認インターフェース │
└────────┬────────────┘
         │  承認済みURLのみ通過
         ▼
┌─────────────────────┐
│  Step 2: catalog.json│  ← インデックスに登録・ステータス管理
│  インデックス管理    │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│  既存パイプライン    │  ← scraper.py → extractor.py（2パス）
│  スクレイプ & 抽出   │
└─────────────────────┘
```

---

### Step 1 ― `src/discovery.py` の新規作成

検索とリソース提案を担うモジュール。

**責務：**
- **検索APIの統合**: Brave Search API / Google Search API を使い、クエリ（例: `openai api pricing 2026`）で検索
- **提案ロジック**: 取得したスニペットから「読む価値があるか」を判定し、`DiscoveryProposal` クラスにマッピング

---

### Step 2 ― `catalog.json` によるインデックス管理

プロジェクトルートに管理帳簿を置き、フォルダ構造の一貫性を保つ。

```json
{
  "last_updated": "2026-05-05",
  "entries": [
    {
      "url": "https://openai.com/blog/...",
      "label": "openai_news",
      "status": "extracted",
      "local_path": "data/raw/openai_news/gpt-5-release.md"
    }
  ]
}
```

**`status` の遷移：**
```
proposed → approved → fetched → extracted
```

---

### Step 3 ― ユーザー承認インターフェース (`main.py` へのサブコマンド追加)

```bash
# 1. URLを探して提案させる
python -m src.main --discover "Anthropicの最新モデルと料金について"

# 2. 提案リストを確認し、全件承認して解析を実行
python -m src.main --approve-all
```

---

## 4. コンテキストエンジニアリング設計

### 4.1 セマンティック・ゲートキーパー（広告・SEOゴミ回避）

検索結果には、アフィリエイト目的のブログや「AIツール10選」のようなSEOゴミが大量に含まれる。
これを排除するため、**「専門家としての選別基準」をプロンプトに埋め込む**。

| シグナル | 判定基準 |
|---|---|
| ✅ Positive（加点） | 公式ドメイン (`docs.`, `blog.openai.com` 等) |
| ✅ Positive（加点） | 具体的なバージョン番号（`v3.5`, `2026-03-10`）やコード片を含む |
| ✅ Positive（加点） | 「廃止予定」「破壊的変更」など技術的具体性に踏み込んでいる |
| ❌ Negative（即排除） | 「完全ガイド」「おすすめ」など一般消費者向けの煽り文句 |
| ❌ Negative（即排除） | 具体的仕様（API, Schema, Benchmark）を含まない価格比較・羅列記事 |
| ❌ Negative（即排除） | 最新情報（2026年現在）に対して明らかに古い日付の情報 |

**実装のポイント：** URLとスニペットをLLMに渡し、`is_technical_resource: bool` だけでなく `quality_score: int (1–10)` を算出させる。

---

### 4.2 動的ラベリングのためのタクソノミー設計

LLMに「現在のフォルダ構造」と「ユーザーの最終目的」を常に意識させる。

`discovery.py` がラベルを生成する際に渡すコンテキスト：

| コンテキスト | 内容 |
|---|---|
| **Research Goal** | 「2026年のAIエコシステムの変化と組み込み開発への影響を調査中」 |
| **Existing Taxonomy** | `catalog.json` から現在のラベル一覧を取得し「可能な限り既存ラベルに寄せよ」と指示 |
| **命名規則** | 製品名含む場合 → `product_name/category`、時系列重要な場合 → `event_2026` のように年号を含める |

---

### 4.3 実装レベルでのコンテキスト活用フロー

`discovery.py` に組み込む判定プロンプトの構成：

```markdown
あなたは高度な技術リサーチャーです。
以下のURL候補を、研究室の共有ライブラリに保存する価値があるか判定してください。

### ユーザーの関心事
- {{ user_query }}

### 排除すべきノイズ（広告回避コンテキスト）
- 一般消費者向けの内容、アフィリエイト、過剰な宣伝
- 具体的仕様（API, Schema, Benchmark）を含まない要約記事

### ラベリングのルール
- 既存ラベル: {{ existing_labels }}
- 新設する場合は、将来的に他のドキュメントも入りそうな汎用的な名称にすること

### 出力
DiscoveryProposal 形式のJSONで出力してください。
```

---

## 5. 実装優先順位

| 優先度 | タスク | 対象ファイル |
|---|---|---|
| 🔴 高 | `DiscoveryProposal` Pydanticモデルの定義 | `src/models.py` |
| 🔴 高 | 検索API統合 + セマンティック評価ロジック | `src/discovery.py`（新規） |
| 🟡 中 | `catalog.json` の読み書き管理 | `src/catalog.py`（新規） |
| 🟡 中 | `--discover` / `--approve-all` サブコマンド追加 | `src/main.py` |
| 🟢 低 | ラベリング精度チューニング（タクソノミー拡張） | `src/discovery.py` |
